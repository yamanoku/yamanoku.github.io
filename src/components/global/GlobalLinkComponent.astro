---
import type { LinkItem } from "./GlobalTypes";

/* eslint-disable-next-line @typescript-eslint/no-unused-vars */
interface Props {
  link: LinkItem;
}

const { link } = Astro.props;
---

<a rel={link.rel} href={link.url}>
  {link.title}
</a>
